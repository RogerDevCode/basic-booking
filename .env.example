# ==========================================
# 🔧 AUTOAGENDA INFRASTRUCTURE CONFIG (.env)
# ==========================================
# Use this file to configure environment-specific settings for Docker and Nginx.
# Copy to .env and adjust values for production.

# --- POSTGRES DATABASE ---
# Connection Pool Settings (Optimized for n8n + Webhook Load)
DB_POSTGRESDB_POOL_MIN=10
DB_POSTGRESDB_POOL_MAX=50
DB_POSTGRESDB_POOL_IDLE_TIMEOUT_MS=600000
DB_POSTGRESDB_CONNECTION_TIMEOUT_MS=30000

# Credentials (Managed via Neon / External Env Vars in Prod)
# POSTGRES_USER=neondb_owner
# POSTGRES_PASSWORD=...
# POSTGRES_DB=neondb
# POSTGRES_HOST=...

# --- N8N WORKFLOW AUTOMATION ---
# Concurrency Limits (Prevent OOM)
N8N_CONCURRENCY_PRODUCTION_LIMIT=50
EXECUTIONS_TIMEOUT=300
EXECUTIONS_TIMEOUT_MAX=3600

# Security
# Encryption key for n8n credentials (DO NOT LOSE THIS)
N8N_ENCRYPTION_KEY=super-secret-encryption-key-change-me

# --- NGINX / WEB SERVER ---
# Rate Limiting (Prevent DDoS)
NGINX_GENERAL_RATE_LIMIT=100r/m
NGINX_API_RATE_LIMIT=50r/m
NGINX_LOGIN_RATE_LIMIT=5r/m

# Timeouts
NGINX_PROXY_CONNECT_TIMEOUT=30s
NGINX_PROXY_READ_TIMEOUT=60s

# --- JWT AUTHENTICATION ---
# Secret for signing dashboard tokens (Must match BB_08 workflow)
JWT_SECRET=super-secret-jwt-key-change-me-in-production-please

# --- TELEGRAM ---
# Bot Token (Required for BB_01 Gateway)
TELEGRAM_BOT_TOKEN=123456789:ABCDefGHIjklMNOpqrsTUVwxyz
