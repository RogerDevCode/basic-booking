{
  "name": "BB_03_04_BookingsData",
  "nodes": [
    {
      "parameters": {
        "httpMethod": "POST",
        "path": "bb03-bookings-data",
        "responseMode": "lastNode",
        "options": {}
      },
      "id": "webhook",
      "name": "\ud83e\uddea Test Webhook",
      "type": "n8n-nodes-base.webhook",
      "typeVersion": 2,
      "position": [
        0,
        300
      ],
      "webhookId": "bb03-bookings-data-test"
    },
    {
      "parameters": {
        "inputSource": "passthrough"
      },
      "id": "trigger",
      "name": "Start",
      "type": "n8n-nodes-base.executeWorkflowTrigger",
      "typeVersion": 1.1,
      "position": [
        0,
        500
      ]
    },
    {
      "parameters": {
        "jsCode": "const { DateTime } = require('luxon');\n\nconst WORKFLOW_ID = 'BB_03_04_BookingsData';\nconst meta = () => ({ source: 'guard', timestamp: DateTime.utc().toISO(), workflow_id: WORKFLOW_ID });\nconst fail = (code, msg) => [{ json: { success: false, error_code: code, error_message: msg, data: null, _meta: meta() } }];\nconst ok = (data) => [{ json: { success: true, error_code: null, error_message: null, data, _meta: meta() } }];\n\ntry {\n  const items = $input.all();\n  if (!items?.length) return fail('VAL_NO_INPUT', 'No input received');\n\n  const raw = items[0].json.body || items[0].json;\n  \n  if (!raw.provider_id) {\n    return fail('VAL_MISSING_ID', 'Missing provider_id in input');\n  }\n\n  return ok({\n    provider_id: raw.provider_id,\n    start_date: raw.start_date || DateTime.utc().toISO().split('T')[0]\n  });\n} catch (e) {\n  return fail('INTERNAL_ERROR', `${WORKFLOW_ID}: ${e.message}`);\n}"
      },
      "id": "guard",
      "name": "Guard",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        220,
        400
      ]
    },
    {
      "parameters": {
        "conditions": {
          "options": {
            "caseSensitive": true,
            "version": 2
          },
          "conditions": [
            {
              "id": "check-success",
              "leftValue": "={{ $json.success }}",
              "operator": {
                "type": "boolean",
                "operation": "true"
              }
            }
          ],
          "combinator": "and"
        }
      },
      "id": "guard_ok",
      "name": "Guard OK?",
      "type": "n8n-nodes-base.if",
      "typeVersion": 2,
      "position": [
        440,
        400
      ]
    },
    {
      "parameters": {
        "operation": "executeQuery",
        "query": "=SELECT \n  id, \n  start_time, \n  end_time, \n  status\nFROM public.bookings \nWHERE provider_id = '{{ $json.data.provider_id }}' \nAND status NOT IN ('cancelled')\nAND deleted_at IS NULL\nORDER BY start_time ASC;"
      },
      "id": "db_get_bookings",
      "name": "DB: Get Bookings",
      "type": "n8n-nodes-base.postgres",
      "typeVersion": 2.6,
      "position": [
        660,
        300
      ],
      "credentials": {
        "postgres": {
          "id": "99BnrzwZQDhYU6Ly"
        }
      },
      "alwaysOutputData": true,
      "onError": "continueRegularOutput"
    },
    {
      "parameters": {
        "jsCode": "const { DateTime } = require('luxon');\n\nconst WORKFLOW_ID = 'BB_03_04_BookingsData';\nconst meta = () => ({ source: 'validate_db', timestamp: DateTime.utc().toISO(), workflow_id: WORKFLOW_ID });\nconst fail = (code, msg) => [{ json: { success: false, error_code: code, error_message: msg, data: null, _meta: meta() } }];\nconst ok = (data) => [{ json: { success: true, error_code: null, error_message: null, data, _meta: meta() } }];\n\ntry {\n  const items = $input.all();\n  const first = items[0].json;\n\n  // Check for DB error\n  if (first.error) {\n    return fail('DB_ERROR', `Database failed: ${first.error.message}`);\n  }\n\n  // For READ operations, empty result is NOT an error, it's an empty list\n  if (!first.id) {\n    return ok([]);\n  }\n\n  // Return all items as a list in 'data'\n  return ok(items.map(i => i.json));\n} catch (e) {\n  return fail('INTERNAL_ERROR', `${WORKFLOW_ID}: ${e.message}`);\n}"
      },
      "id": "validate_db",
      "name": "Validate DB Result",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        880,
        300
      ]
    },
    {
      "parameters": {
        "jsCode": "const { DateTime } = require('luxon');\n\nconst WORKFLOW_ID = 'BB_03_04_BookingsData';\nconst meta = () => ({ source: 'format_output', timestamp: DateTime.utc().toISO(), workflow_id: WORKFLOW_ID });\n\ntry {\n  const raw = $input.first()?.json;\n  return [{ \n    json: {\n      success: raw.success !== false,\n      error_code: raw.error_code || null,\n      error_message: raw.error_message || null,\n      data: raw.data || (raw.success === false ? null : raw),\n      _meta: meta()\n    }\n  }];\n} catch (e) {\n  return [{ json: { success: false, error_code: 'INTERNAL_ERROR', error_message: e.message, data: null, _meta: meta() } }];\n}"
      },
      "id": "format_output",
      "name": "Format Output",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        1100,
        400
      ]
    }
  ],
  "connections": {
    "\ud83e\uddea Test Webhook": {
      "main": [
        [
          {
            "node": "Guard",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "trigger": {
      "main": [
        [
          {
            "node": "Guard",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Guard": {
      "main": [
        [
          {
            "node": "Guard_ok",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Guard_ok": {
      "main": [
        [
          {
            "node": "format_output",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "db_get_bookings",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "db_get_bookings": {
      "main": [
        [
          {
            "node": "validate_db",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "validate_db": {
      "main": [
        [
          {
            "node": "format_output",
            "type": "main",
            "index": 0
          }
        ]
      ]
    }
  },
  "settings": {
    "executionOrder": "v1",
    "saveManualExecutions": true,
    "callerPolicy": "workflowsFromSameOwner",
    "errorWorkflow": "BB_00_Global_Error_Handler"
  }
}